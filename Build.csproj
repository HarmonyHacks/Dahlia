<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Target Name="Build">
    <Exec Command="powershell -ExecutionPolicy Bypass -File .\lib\psake\psake.ps1 build.ps1" />
  </Target>
</Project>
